thistlist = [1, 2, 3, 4, 5]
print(thistlist)
#nesting of list 
nestedlist = ["mouse", [8, 4, 6], ['a']]
print(nestedlist)
"""
list do use square brackets []
    to denote a list.
    Lists are mutable, meaning they can be changed.
    Lists can contain items of different data types.
    Lists can be nested, meaning they can contain other lists as items.
    
"""